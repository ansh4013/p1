/* ============================================================
   senior/ch16-logarithms.js
   ============================================================ */
SeniorChapters.push({name:'Logarithms',icon:'㏒',boardTitle:'log_b(x) = y',theory(){return`<h4>㏒ Logarithms</h4><p>Logarithm is the <b>inverse of exponentiation</b>.</p><div class="formula">log_b(x) = y ↔ b^y = x</div><div class="formula">log(xy) = log(x)+log(y) &nbsp;|&nbsp; log(x/y) = log(x)−log(y)</div><div class="formula">log(xⁿ) = n·log(x) &nbsp;|&nbsp; Change of base: log_b(x) = ln(x)/ln(b)</div>`;},renderForm(c){c.innerHTML=`<label>Mode</label><select id="slogMode"><option value="eval">Evaluate log_b(x)</option><option value="solve">Solve log_b(x) = y → find x</option></select><div class="input-row"><div><label>Base b</label><input type="number" id="slogB" value="10" min="0.001" step="any"/></div><div><label>x (or y for solve)</label><input type="number" id="slogX" value="1000" step="any"/></div></div><button class="solve-btn" onclick="SeniorCh16.solve()">㏒ Calculate!</button>`;}});
const SeniorCh16={solve(){const mode=document.getElementById('slogMode').value,b=parseFloat(document.getElementById('slogB').value)||10,x=parseFloat(document.getElementById('slogX').value)||100;if(b<=0||b===1){Teacher.say("Base must be positive and not equal to 1! 😬",2500);return;}if(mode==='eval'){if(x<=0){Teacher.say("Logarithm argument must be positive! 😊",2500);return;}const result=parseFloat((Math.log(x)/Math.log(b)).toFixed(6));const steps=[{type:'teacher-say',speak:`log base ${b} of ${x}! Let's calculate!`,html:`🎤 Evaluate: <b>log₍${b}₎(${x})</b>`,delay:0,board:`log_${b}(${x})`},{type:'highlight',speak:`Using change of base: log_b(x) = ln(x)/ln(b) = ${Math.log(x).toFixed(4)}/${Math.log(b).toFixed(4)}`,html:`<b>Change of base:</b> ln(${x})/ln(${b}) = ${Math.log(x).toFixed(4)}/${Math.log(b).toFixed(4)}`,delay:900},{type:'result',html:`🎉 log₍${b}₎(${x}) = <b>${result}</b><br>Verify: ${b}^${result} ≈ ${parseFloat(Math.pow(b,result).toFixed(4))} ✓`,speak:`log base ${b} of ${x} equals ${result}! 🌟`,delay:1800,board:`= ${result}`}];App.showSteps(steps);}else{const xResult=parseFloat(Math.pow(b,x).toFixed(6));const steps=[{type:'teacher-say',speak:`If log_${b}(x) = ${x}, then x = ${b}^${x} = ${xResult}!`,html:`🎤 log₍${b}₎(x) = ${x} → x = ${b}^${x}`,delay:0,board:`b^y = x`},{type:'result',html:`🎉 x = ${b}^${x} = <b>${xResult}</b>`,speak:`x equals ${xResult}! The key rule: log_b(x)=y means b to the y = x! 🌟`,delay:900,board:`x = ${xResult}`}];App.showSteps(steps);}}};
